{
  "name": "goldhunter",
  "version": "1.0.0",
  "description": "A 2.5 minute, 4-player gold rush brawler that runs in a single HTML file.",
  "private": true,
  "scripts": {
    "build": "node tools/build.js",
    "dev": "npx --yes http-server . -p 8080 -c-1 -o /index.html",
    "test": "npm run build && node tools/smoke.js --matches 6 && node tools/input-test.js",
    "balance": "npm run build && node tools/smoke.js --matches 12 --verbose",
    "shots": "npm run build && node tools/shots.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
